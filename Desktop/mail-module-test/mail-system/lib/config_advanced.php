<?php
/**
 * 高度な設定ファイル（技術者向け）
 *
 * このファイルは技術者向けの設定です。
 * 通常は変更する必要はありません。
 */

// ========================================
// 送信元設定
// ========================================
// SPF認証対応の送信者メールアドレスは自動生成されます
// 例: noreply@your-domain.com

// ローカル環境用のデフォルトドメイン
// 本番環境では $_SERVER['HTTP_HOST'] から自動取得されます
$default_domain = 'example.com';

// ========================================
// セキュリティ設定
// ========================================
// レート制限（1時間あたりの最大送信回数）
$rate_limit_max = 50;

// タイムスタンプチェック（秒）
// フォーム表示から送信までの最小時間（bot対策）
$min_submit_time = 3;

// ========================================
// リダイレクト設定
// ========================================
// 送信完了後に戻るトップページのURL
// getTopPageUrl()関数で自動生成される絶対URLを使用
// SPF認証と同じドメイン情報を流用するため、環境に依存せず動作します
// 注意: この変数は thanks.php で email_helper.php 読み込み後に再定義されます
$top_page_url = '../../index.html'; // フォールバック用（通常は使用されません）

// ========================================
// デバッグ設定
// ========================================
// true にすると詳細なログが出力されます
$debug_mode = false; // 本番環境にアップする際は false にしてください

if ($debug_mode) {
    error_reporting(E_ALL);
    ini_set("display_errors", 1);
    ini_set("log_errors", 1);
    ini_set("error_log", dirname(__DIR__) . "/error.log");
}
